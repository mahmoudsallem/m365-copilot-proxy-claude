{
  "schema": "myclaude.execution-profiles/v1",
  "default": "guarded",
  "profiles": {
    "guarded": {
      "description": "Workspace-scoped built-in file tools plus best-effort Bash denials and Claude Code approvals; this is defense in depth, not a sandbox.",
      "preToolUse": "enforce",
      "stopVerificationGate": true
    },
    "host-unrestricted": {
      "description": "Observe and audit the guarded rules without changing Claude Code's own permission decision.",
      "preToolUse": "observe-only",
      "stopVerificationGate": true
    }
  }
}
